/** @type {import('next').NextConfig} */
const nextConfig = {
    experimental: {
      scrollRestoration: true, // Enable experimental scroll restoration
    },
    // Optional: If you're using app directory (recommended)
    reactStrictMode: true,
  };
  
  export default nextConfig;